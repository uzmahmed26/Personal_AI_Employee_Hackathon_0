# Personal AI Employee — Control Center

> [!tip] Live Dashboard — powered by Dataview
> This page auto-updates as tasks move through the pipeline. Install the **Dataview** plugin to see live queries.

---

## Task Pipeline

### 🟡 Incoming Tasks
```dataview
TABLE task_id, priority, type, created
FROM "01_Incoming_Tasks"
WHERE file.name != ".gitkeep"
SORT created DESC
```

### 🔵 In Progress
```dataview
TABLE task_id, priority, type, created
FROM "02_In_Progress_Tasks"
WHERE file.name != ".gitkeep"
SORT priority DESC
```

### 🔴 Needs Approval
```dataview
TABLE task_id, priority, type, created
FROM "Pending_Approval"
WHERE file.name != ".gitkeep"
SORT created DESC
```

### ✅ Completed (last 7 days)
```dataview
TABLE task_id, priority, type, created
FROM "03_Completed_Tasks"
WHERE file.name != ".gitkeep"
SORT created DESC
LIMIT 10
```

### ❌ Failed Tasks
```dataview
TABLE task_id, priority, type, created
FROM "05_Failed_Tasks"
WHERE file.name != ".gitkeep"
SORT created DESC
```

---

## Active Plans
```dataview
TABLE file.mtime AS "Last Updated"
FROM "Plans"
WHERE file.name != ".gitkeep"
SORT file.mtime DESC
```

---

## Decision Ledger (Recent)
```dataview
TABLE file.mtime AS "Date"
FROM "Decision_Ledger"
WHERE file.name != ".gitkeep"
SORT file.mtime DESC
LIMIT 5
```

---

## Social Media Queues

### Drafts Pending
```dataview
TABLE file.folder AS "Platform", file.mtime AS "Drafted"
FROM "Business"
WHERE contains(file.name, "DRAFT_")
SORT file.mtime DESC
```

### Published Posts
```dataview
TABLE file.folder AS "Platform", file.mtime AS "Posted"
FROM "Business"
WHERE contains(file.name, "POSTED_")
SORT file.mtime DESC
LIMIT 10
```

---

## Quick Navigation

| Section | Link |
|---------|------|
| Static Dashboard | [[Dashboard]] |
| Business Goals | [[Business_Goals]] |
| Company Handbook | [[Company_Handbook]] |
| Kanban Board | [[Kanban]] |
| Weekly Report | [[Reports/Weekly_CEO_Report]] |
| Memory | [[Memory/README]] |

---

## Quick Commands

```bash
# Update static Dashboard.md
python dashboard_updater.py

# Process all pending tasks (Ralph Loop)
python ralph_wiggum_hook.py --start --prompt "Process all pending tasks"

# Generate CEO briefing
python weekly_ceo_report.py

# Auto-generate plans
python plan_generator.py --auto

# Create a new task
python run_task_skill.py create -t email -d "Description" -p high
```

---

*Open in Obsidian with **Dataview** plugin for live task queries.*
*[[Dashboard|Static Dashboard]] is auto-generated by `dashboard_updater.py`*
